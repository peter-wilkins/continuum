# Issue 020: Show Public MVP Git Hash

Status: done

## Goal

Make it obvious which build a tester is looking at.

## Scope

- Reuse the existing Vite `VITE_COMMIT_HASH` build define.
- Show `Git <hash>` on the index and public Continuum pages.

## Acceptance Criteria

- [x] The MVP index shows the current git hash.
- [x] The Ada public Continuum shows the current git hash.
- [x] The Lens guide shows the current git hash.
